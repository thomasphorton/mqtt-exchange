{
  "name": "mqtt-exchange",
  "version": "1.0.2",
  "description": "Listens to a local MQTT broker and republishes messages to AWS IoT.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-iot-device-sdk": "^2.2.1",
    "mqtt": "^4.3.7"
  }
}
